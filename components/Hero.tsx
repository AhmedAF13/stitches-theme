import React, { FC } from "react";
import Container from "@UI/Container";

interface Props {}

const Hero: FC<Props> = (props) => {
  return (
    <div>
      <Container>
          Hero
      </Container>
    </div>
  );
};

export default Hero;
